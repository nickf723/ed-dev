# Quadratic Equations Unit Visual Verification

This file extends the repository visual-verification queue while Vercel previews are unavailable. A passing TypeScript build is not a visual approval.

## Unit map

**Route:** `/formal-science/mathematics/algebra/elementary-algebra/quadratic-equations`

Verify:

- five lessons appear in the intended sequence and all read as live
- the throughline is clearer than the lesson-card controls
- long specimens such as `x² + bx + (b/2)²` stay inside their cards
- the old all-in-one lesson layout is fully replaced by the unit map
- desktop and narrower layouts preserve card balance

## Quadratic Patterns & Parabolas

**Route:** `/formal-science/mathematics/algebra/elementary-algebra/quadratic-equations/patterns-parabolas`

Verify:

- the table → first differences → second differences sequence reads vertically without horizontal clipping
- all three cases classify correctly as linear, quadratic, or neither
- rows with five, four, and three values remain aligned and readable
- the worked parabola stays inside the graph viewport
- negative-leading-coefficient boundary copy remains subordinate to the primary detector

## Vertex Form & Transformations

**Route:** `/formal-science/mathematics/algebra/elementary-algebra/quadratic-equations/vertex-form`

Verify:

- changing `a`, `h`, or `k` truly holds the other two parameters fixed
- every discrete parameter preset stays inside a useful graph window
- the h-sign warning is visible without overpowering the transformation lab
- vertex, axis, opening, and width readouts remain synchronized
- parameter controls wrap cleanly at narrower desktop widths

## Roots & X-Intercepts

**Route:** `/formal-science/mathematics/algebra/elementary-algebra/quadratic-equations/roots-intercepts`

Verify:

- two-root, repeated-root, and no-real-root graphs are visually distinct
- root markers remain hidden until the correct classification is chosen
- repeated-root geometry reads as touch-and-turn rather than an omitted second point
- the no-real-root curve remains fully visible above the x-axis
- factor equations and feedback do not overflow their side panel

## Completing the Square

**Route:** `/formal-science/mathematics/algebra/elementary-algebra/quadratic-equations/completing-square`

Verify:

- half → square → add both sides → rewrite is visually unmistakable
- the leading-coefficient case clearly normalizes before asking for the missing term
- wrong choices do not reveal the completed-square result
- balanced equation, square form, and final solution fit at narrow widths
- the `±` step remains connected to undoing the square, not to the construction step

## Quadratic Formula & Discriminant

**Route:** `/formal-science/mathematics/algebra/elementary-algebra/quadratic-equations/quadratic-formula`

Verify:

- coefficient mapping preserves negative signs
- positive, zero, and negative discriminant cases match two, one, and no real roots
- roots appear on the graph only after a correct forecast
- formula substitution keeps the whole numerator over `2a`
- the negative-discriminant explanation reads as a boundary of the real-number system, not as a broken equation

**Status:** Rendered verification required for all six routes.
