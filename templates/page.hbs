"use client";
import React from "react";
import { useAppStore } from "@/lib/store";
import { Button } from "@/components/ui/Button";
import { Panel } from "@/components/ui/Panel";
import { Layers } from "lucide-react";

export default function {{pascalCase name}}Page() {
  return (
    <div className="p-8 md:p-12 min-h-screen space-y-8">
      <header className="flex flex-col gap-2">
        <div className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-cyan-500">
          <Layers size={14} />
          Domain: {{titleCase name}}
        </div>
        <h1 className="text-4xl font-black text-white tracking-tighter">
          {{upperCase name}} <span className="text-slate-600">HUB</span>
        </h1>
      </header>

      <Panel title="Main Content">
        <div className="min-h-[200px] flex items-center justify-center text-slate-500 text-sm">
          System ready. Begin injecting content for {{titleCase name}}.
        </div>
      </Panel>
    </div>
  );
}